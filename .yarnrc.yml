enableScripts: true

nodeLinker: node-modules

# We publish @fastnear ourselves — don't age-gate our own releases, or
# same-day cross-repo bumps (e.g. near-connect -> wallet) can't resolve.
npmScopes:
  fastnear:
    npmMinimalAgeGate: 0
